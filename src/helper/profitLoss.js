// given purchase price and current price, return profit/loss amount

function profitLoss(purchasePrice, currentPrice) {
  const profitLoss = currentPrice - purchasePrice;
  return profitLoss;
}
export default profitLoss;
